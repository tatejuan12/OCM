export declare type verifySignatureResult = {
    signedBy: string;
    signatureValid: boolean;
    signatureMultiSign: boolean;
};
export declare const verifySignature: (txBlob: string, explicitMultiSigner?: string | undefined) => verifySignatureResult;
