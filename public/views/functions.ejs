<%
nftLiked = function() {
    try {
        var checker = false;
        nft.likes.forEach(function(like){
        if (like == wallet) checker = true;
        }); 
        return checker;
    } catch (error) {
        return checker;
    }
}
%>